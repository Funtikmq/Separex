function Section({ style, onClick }) {
    return (
        <div
            className="doorSection"
            style={style}
            onClick={onClick}
        ></div>
    );
}

export default Section;